# Game01 — 镭射闯关模式 Maze 模块开发文档

> 本文档以 **Maze00 ~ Maze09** 为模块单元，逐一解析 Game01（镭射闯关模式）中每个迷宫关卡的激光灯布局、运动方式与控制参数。
>
> **核心代码文件：**
>
> | 文件 | 职责 |
> |---|---|
> | `Assets/Scripts/Game/Game_Map/Game_Map01.cs` | 迷宫初始化入口，定义 `Initmap_Maze_00()` ~ `Initmap_Maze_09()` |
> | `Assets/Scripts/Game/Game_Map/Game01_Ledone.cs` | 单个激光对象的运动、渲染与碰撞检测 |
> | `Assets/Scripts/Game/Game01/Game01_Player.cs` | 玩家阶段管理，`currJieDuan` 控制 4 阶段循环 |
> | `Assets/Scripts/Game/Game01/Game01_Main.cs` | 主控制器，`index_JieDuan` 控制大轮次 |
> | `Assets/Scripts/Game/Game01/GameLeiSheLedControl.cs` | 预设图案与动画系统 |

---

## 目录

- [一、全局机制](#一全局机制)
  - [1.1 迷宫选择机制](#11-迷宫选择机制)
  - [1.2 坐标系统](#12-坐标系统)
  - [1.3 四阶段循环 (currJieDuan)](#13-四阶段循环-currjieduan)
  - [1.4 运动类型 (en_Move_Type)](#14-运动类型-en_move_type)
  - [1.5 节拍系统 (max_runtime)](#15-节拍系统-max_runtime)
  - [1.6 地面激光 (Ground Laser)](#16-地面激光-ground-laser)
  - [1.7 碰撞与保护时间](#17-碰撞与保护时间)
  - [1.8 Jieduan_Now 分支说明](#18-jieduan_now-分支说明)
- [二、Maze00 — 散点入门](#二maze00--散点入门)
- [三、Maze01 — 双色块静态](#三maze01--双色块静态)
- [四、Maze02 — 双色块平移](#四maze02--双色块平移)
- [五、Maze03 — 大面积遮挡](#五maze03--大面积遮挡)
- [六、Maze04 — 天花板 + 地面巡逻](#六maze04--天花板--地面巡逻)
- [七、Maze05 — 柱阵 + 右侧巡逻](#七maze05--柱阵--右侧巡逻)
- [八、Maze06 — 高速列墙](#八maze06--高速列墙)
- [九、Maze07 — 天花板 + 中心散射](#九maze07--天花板--中心散射)
- [十、Maze08 — 三段漂移 + 地柱](#十maze08--三段漂移--地柱)
- [十一、Maze09 — 闪烁围栏](#十一maze09--闪烁围栏)
- [十二、参数速查表](#十二参数速查表)

---

## 一、全局机制

### 1.1 迷宫选择机制

```csharp
// Game_Map01.cs → Initmap()
Invoke("Initmap_Maze_" + (Main.MapIndex % 12).ToString("D2"), 0);
```

- 由 `Main.MapIndex % 12` 决定调用哪个迷宫初始化方法
- 目前只实现了 `Initmap_Maze_00` ~ `Initmap_Maze_09`（共 10 个）
- `MapIndex` 为 10 或 11 时，`Invoke` 找不到对应方法，会静默失败（不创建任何激光对象）
- `Initmap()` 在 `Game01_Main.GameStart()` 中被调用，每局游戏开始时执行一次

### 1.2 坐标系统

```
y = Height-1  ┌──────────────────────────────────┐  ← 顶部（天花板）
               │                                  │
               │         激光矩阵区域              │
               │                                  │
y = 0          └──────────────────────────────────┘  ← 底部（地面）
               x=0                          x=Width-1
```

- **x 轴**：0（左）→ `Width - 1`（右），水平方向
- **y 轴**：0（底/地面）→ `Height - 1`（顶/天花板），垂直方向
- `Width` 和 `Height` 由 `Set.setVal` 提供，为硬件配置决定的矩阵尺寸

### 1.3 四阶段循环 (currJieDuan)

每一局游戏包含 **4 个阶段**（`currJieDuan` = 0 → 1 → 2 → 3），交替进行 PPD 目标拍击和激光闪避：

| 阶段 | currJieDuan | 类型 | 激光行为 | 玩家任务 |
|---|---|---|---|---|
| PPD-1 | 0 | 目标拍击 | 地面全亮（Ground Laser） | 拍击通道4上的目标 LED |
| 闪避-1 | 1 | 激光闪避 | `Game01_Ledone` 对象激活运动 | 闪避激光，寻找终点按钮 |
| PPD-2 | 2 | 目标拍击 | 地面全亮（Ground Laser） | 拍击通道5上的目标 LED |
| 闪避-2 | 3 | 激光闪避 | `Game01_Ledone` 对象激活运动 | 闪避激光，寻找终点按钮 |

**关键控制代码：**

```csharp
// Game01_Ledone.cs → Update()
if (Game01_Main.instance.player.currJieDuan != 1 && Game01_Main.instance.player.currJieDuan != 3)
    return; // 只在阶段 1 和 3 执行 Moving()
Moving();
```

```csharp
// Game_Map01.cs → Update()
if (Game01_Main.instance.player.currJieDuan == 0 || Game01_Main.instance.player.currJieDuan == 2)
{
    // 地面全亮渲染 + 碰撞检测
}
```

> **重点**：Maze 中创建的所有 `Game01_Ledone` 对象在 `currJieDuan == 0/2` 时完全静止且不渲染（不执行 `Moving()`）。只有在 `currJieDuan == 1/3` 时才开始运动和碰撞检测。

### 1.4 运动类型 (en_Move_Type)

`Game01_Ledone` 定义了 6 种运动类型：

| 枚举值 | 名称 | 行为描述 |
|---|---|---|
| 0 | **Stay** | 固定不动。位置在初始化时确定后不再改变。 |
| 1 | **Shake** | 闪烁。每个节拍交替 `isShaking = true/false`，true 时渲染+检测，false 时隐藏。 |
| 2 | **OneWay** | 单向循环平移。`dir=0` 时所有点 x+1（向右），到达右边界回绕到 x=0；`dir=1` 时反向。 |
| 3 | **Down** | 上下往返。y 坐标在 `donw_Y` 和 `Height` 之间来回移动，每步之后停留 1 秒。 |
| 4 | **Up** | 上下往返。行为与 Down 相同（代码逻辑一致）。 |
| 5 | **Trun** | 预留未实现。switch 中为空 break。 |

**OneWay 的运动公式：**

```
每个节拍 (runtime > max_runtime):
  对 pos_group 中每个点:
    dir=0: x = (x + 1) % Width    → 向右平移，右边界回绕
    dir=1: x = (x - 1 + Width) % Width  → 向左平移，左边界回绕
```

> 注意：所有 Maze 初始化中 `dir` 参数均为 `0`，因此 OneWay 始终 **向右** 移动。

### 1.5 节拍系统 (max_runtime)

`max_runtime` 是全局移动节拍间隔（秒），控制所有 `Game01_Ledone` 对象的移动频率：

```csharp
// Game01_Ledone.cs → Moving()
if (runtime > Game_Map01.instance.max_runtime)
{
    runtime = 0;
    // 执行一步运动
}
```

- 值越小，移动频率越高（速度越快）
- 每个 Maze 在初始化时设置自己的 `max_runtime`
- 是**全局共享**的，一个 Maze 中所有激光对象使用同一个节拍

### 1.6 地面激光 (Ground Laser)

在 `currJieDuan == 0` 和 `currJieDuan == 2` 时，`Game_Map01.Update()` 会：

1. **全屏点亮**：遍历整个 `Width x Height` 矩阵，将所有 LED 设为 Target 状态（亮度 255）
2. **碰撞检测**：仅对 **x=0 列** 执行按键检测，未按下则扣血

```csharp
// Game_Map01.cs → Update()
for (int i = 0; i < Set.setVal.Width; i++)
{
    for (int k = 0; k < Set.setVal.Height; k++)
    {
        Framebuffer.Update_TransmitLedColor(i, k, 255, enPointSta.Target);
        if (i == 0)  // 仅第一列检测碰撞
        {
            if (LedKey.KeyStatus(...) == false)
            {
                // 扣血逻辑，保护时间 1 秒
            }
        }
    }
}
```

地面激光与 Maze 模式的激光对象**互斥**：PPD 阶段全亮地面，闪避阶段运行 Maze 激光。

### 1.7 碰撞与保护时间

游戏中存在多层保护时间机制：

| 保护类型 | 变量 | 触发条件 | 持续时间 | 作用域 |
|---|---|---|---|---|
| 初始化保护 | `Game_Map01.protectTime` | `Initmap()` 调用 | 5 秒 | 全局 |
| Ledone 创建保护 | `Game_Map01.protectTime` | 每个 `Game01_Ledone.Init()` | 4 秒 | 全局 |
| 移动节拍保护 | `Game_Map01.protectTime` | 每次移动步执行 | 0.4 秒 | 全局 |
| Ledone 扣血后保护 | `Game_Map01.protectTime` | 被激光击中 | 3 秒 | 全局 |
| 地面扣血后保护 | `Game_Map01.protectTime` | 地面检测扣血 | 1 秒 | 全局 |

> 所有保护时间共用 `Game_Map01.instance.protectTime`，后设置的值会覆盖前值。

### 1.8 Jieduan_Now 分支说明

`Game_Map01.Jieduan_Now` 是一个 `public int` 字段（默认值 0），在 Maze00、Maze01、Maze02 中用于 `switch(Jieduan_Now)` 分支。

**重要发现**：在当前代码中，`Jieduan_Now` **从未在代码中被赋值修改**（仅声明时初始化为 0）。因此：

- switch 中的 `case 1` / `case 2` / `case 3` 在默认情况下**不会执行**
- 这些分支可能是通过 **Unity Inspector** 手动配置的难度扩展
- 只有 switch **之前**的代码是默认生效的

> 在下文各 Maze 的分析中，我们会分别标注「**默认生效**」和「**Jieduan_Now 扩展**」的内容。

---

## 二、Maze00 — 散点入门

**代码位置**：`Game_Map01.cs` → `Initmap_Maze_00()`（第 60~130 行）

### 全局参数

| 参数 | 值 |
|---|---|
| `max_runtime` | **1.5 秒** |
| 节拍含义 | 每 1.5 秒执行一步运动 |

### 默认激光布局（Jieduan_Now = 0）

创建 **5 个单点 Stay 激光**，固定不动：

| 编号 | 坐标 | 运动类型 | 说明 |
|---|---|---|---|
| 1 | `(5, 0)` | Stay | 地面左侧区域 |
| 2 | `(Width-2, 3)` | Stay | 右侧偏上 |
| 3 | `(7, Height-2)` | Stay | 顶部偏左 |
| 4 | `(9, Height-2)` | Stay | 顶部偏右 |
| 5 | `(Width/2, 3)` | Stay | 中部偏上 |

**布局示意图**（以 Width=20, Height=6 为例）：

```
y=5 (H-1) │ . . . . . . . * . * . . . . . . . . . . │  ← LED 3,4 在 y=H-2=4 位置
y=4 (H-2) │ . . . . . . . * . * . . . . . . . . . . │
y=3       │ . . . . . . . . . . * . . . . . . . * . │  ← LED 2(W-2,3), LED 5(W/2,3)
y=2       │ . . . . . . . . . . . . . . . . . . . . │
y=1       │ . . . . . . . . . . . . . . . . . . . . │
y=0       │ . . . . . * . . . . . . . . . . . . . . │  ← LED 1(5,0)
           x=0                                  x=19
```

> 这是最简单的迷宫，仅有几个固定障碍点，玩家有大量空间闪避。

### Jieduan_Now 扩展分支

<details>
<summary>展开查看 Jieduan_Now = 1/2/3 的扩展内容</summary>

**case 1 — 增加平移方块：**

在基础 5 点之外，额外创建：
- 两个 3x2 的 **OneWay** 方块（顶部区域，向右循环平移）
  - 方块 A：x∈[0,2], y∈[Height-2, Height-1]
  - 方块 B：x∈[Width/2, Width/2+2], y∈[Height-2, Height-1]
- 两个单点 **Stay** 激光：(Width/4, 0) 和 (Width*3/4, 0)

**case 2 — 增加中央大块：**

- 一个大面积 **Stay** 方块：x∈[2, Width-3], y∈[2, Height-2]
- 覆盖矩阵中央大部分区域

**case 3 — 大面积 + 角落闪烁：**

- 一个大面积 **Stay** 方块：x∈[0, Width-1], y∈[3, Height-2]
- 一个角落 **Shake** 方块：左下 2x3 区域 + 右下 2x3 区域
  - 左下：x∈[0,1], y∈[0,2]
  - 右下：x∈[Width-2, Width-1], y∈[0,2]

</details>

---

## 三、Maze01 — 双色块静态

**代码位置**：`Game_Map01.cs` → `Initmap_Maze_01()`（第 131~207 行）

### 全局参数

| 参数 | 值 |
|---|---|
| `max_runtime` | **1.5 秒** |

### 默认激光布局（Jieduan_Now = 0）

创建 **4 个激光对象**，全部为 Stay 类型：

| 编号 | 类型 | 坐标范围 | 运动类型 | 说明 |
|---|---|---|---|---|
| 1 | 多点组 | x∈[0,2], y∈[Height-2, Height-1] | Stay | 左上角 3x2 方块 |
| 2 | 多点组 | x∈[Width/2, Width/2+2], y∈[Height-2, Height-1] | Stay | 中上 3x2 方块 |
| 3 | 单点 | (Width/4, 0) | Stay | 地面 1/4 处 |
| 4 | 单点 | (Width*3/4, 0) | Stay | 地面 3/4 处 |

**布局示意图**（Width=20, Height=6）：

```
y=5 │ * * * . . . . . . . * * * . . . . . . . │
y=4 │ * * * . . . . . . . * * * . . . . . . . │  ← 两个 3x2 Stay 方块
y=3 │ . . . . . . . . . . . . . . . . . . . . │
y=2 │ . . . . . . . . . . . . . . . . . . . . │
y=1 │ . . . . . . . . . . . . . . . . . . . . │
y=0 │ . . . . . * . . . . . . . . . * . . . . │  ← 两个地面单点
     x=0              x=10              x=19
```

> 全部固定不动，作为静态障碍。玩家需要从方块间的间隙通过。

### Jieduan_Now 扩展分支

<details>
<summary>展开查看 Jieduan_Now = 1/2/3 的扩展内容</summary>

**case 1 — 增加平移方块 + 地面点：**
- 额外两个 3x2 **OneWay** 方块（与基础方块坐标重叠，叠加后一起向右移动）
- 额外两个单点 Stay：(Width/4, 0), (Width*3/4, 0)

**case 2 — 增加中央大块：**
- 一个大面积 Stay 方块：x∈[2, Width-3], y∈[2, Height-2]

**case 3 — 大面积 + 角落闪烁：**
- 一个大面积 Stay 方块：x∈[0, Width-1], y∈[3, Height-2]
- 一个角落 Shake 方块（左下 + 右下）

</details>

---

## 四、Maze02 — 双色块平移

**代码位置**：`Game_Map01.cs` → `Initmap_Maze_02()`（第 208~272 行）

### 全局参数

| 参数 | 值 |
|---|---|
| `max_runtime` | **1.5 秒** |

### 默认激光布局（Jieduan_Now = 0）

与 Maze01 结构相似，但基础方块从 Stay 变为 **OneWay**：

| 编号 | 类型 | 坐标范围 | 运动类型 | 说明 |
|---|---|---|---|---|
| 1 | 多点组 | x∈[0,2], y∈[Height-2, Height-1] | **OneWay** | 左上角 3x2 方块，**向右平移** |
| 2 | 多点组 | x∈[Width/2, Width/2+2], y∈[Height-2, Height-1] | **OneWay** | 中上 3x2 方块，**向右平移** |
| 3 | 单点 | (Width/4, 0) | Stay | 地面 1/4 处固定 |
| 4 | 单点 | (Width*3/4, 0) | Stay | 地面 3/4 处固定 |

**运动行为：**

- 方块 1 和 2 以 **1.5 秒/步** 的速度向右平移
- 每步所有点的 x 坐标 +1，到达右边界后回绕到 x=0
- 方块形状在移动过程中保持不变（整体平移）
- 地面两个单点不动

**布局变化示意（t=0 → t=1.5s → t=3s）：**

```
t=0:    │>>>...........>>>..........│   ← 两个 3x2 方块初始位置
t=1.5s: │.>>>...........>>>.........│   ← 整体右移 1 格
t=3.0s: │..>>>...........>>>........│   ← 继续右移
...循环回绕...
```

### Jieduan_Now 扩展分支

<details>
<summary>展开查看 Jieduan_Now = 1/2/3 的扩展内容</summary>

**case 1 — 空分支**（无额外激光）

**case 2 — 增加中央大块：**
- 一个大面积 Stay 方块：x∈[2, Width-3], y∈[2, Height-2]

**case 3 — 大面积 + 角落闪烁：**
- 一个大面积 Stay 方块 + 角落 Shake 方块（与 Maze00/01 的 case 3 结构相同）

</details>

---

## 五、Maze03 — 大面积遮挡

**代码位置**：`Game_Map01.cs` → `Initmap_Maze_03()`（第 273~309 行）

### 全局参数

| 参数 | 值 |
|---|---|
| `max_runtime` | **1.5 秒** |

### 激光布局（无 Jieduan_Now 分支）

创建 **2 个激光对象**：

| 编号 | 坐标范围 | 运动类型 | 说明 |
|---|---|---|---|
| 1 | 顶部 3x2 方块 + 全宽中间带 | **Stay** | 见下方详细说明 |
| 2 | 左下角 + 右下角 | **Shake** | 角落闪烁警示区 |

**对象 1 的坐标构成（合并为一个 pos_group）：**

1. 左上角 3x2：x∈[0,2], y∈[Height-2, Height-1]
2. _(pos1 变量生成但未使用为独立对象)_
3. 全宽中间带：x∈[0, Width-1], y∈[3, Height-2]

> 注意：pos1 生成了 Width/2 偏移的 3x2 点，但这些点只是生成了变量而已，没有用 `New_LedOne(pos1, ...)` 创建。实际合并到 pos 中的只有第一部分和第三部分。

**对象 2 的坐标构成：**

- 左下角：x∈[0,1], y∈[0,2]（2 列 x 3 行 = 6 点）
- 右下角：x∈[Width-2, Width-1], y∈[0,2]（2 列 x 3 行 = 6 点）
- 共 12 个点，以 Shake 模式闪烁

**布局示意图**（Width=20, Height=6）：

```
y=5 │ * * * . . . . . . . . . . . . . . . . . │
y=4 │ * * * * * * * * * * * * * * * * * * * * │  ← 全宽 Stay 带
y=3 │ * * * * * * * * * * * * * * * * * * * * │  ← 全宽 Stay 带
y=2 │ ~ ~ . . . . . . . . . . . . . . . . ~ ~ │  ← ~ = Shake 闪烁
y=1 │ ~ ~ . . . . . . . . . . . . . . . . ~ ~ │
y=0 │ ~ ~ . . . . . . . . . . . . . . . . ~ ~ │
     x=0                                  x=19
```

**运动行为：**

- 对象 1（Stay）：完全静止，覆盖大面积区域，仅留下底部中间通道
- 对象 2（Shake）：每 1.5 秒交替闪烁一次，增加角落区域的不确定性
- 玩家安全区域仅限 **底部中间**（x∈[2, Width-3], y∈[0,2]，且需注意 Shake 闪烁时机）

---

## 六、Maze04 — 天花板 + 地面巡逻

**代码位置**：`Game_Map01.cs` → `Initmap_Maze_04()`（第 310~332 行）

### 全局参数

| 参数 | 值 |
|---|---|
| `max_runtime` | **1.5 秒** |

### 激光布局（无 Jieduan_Now 分支）

| 编号 | 坐标范围 | 运动类型 | 说明 |
|---|---|---|---|
| 1 | 全宽顶部 2 行：x∈[0, Width-1], y∈[Height-2, Height-1] | **Stay** | 天花板固定障碍 |
| 2~N | 地面 x%7==0 处：(0,0), (7,0), (14,0), ... | **OneWay** | 地面巡逻点 |

**地面巡逻点数量**：`floor(Width / 7) + 1` 个（取决于 Width 值）

**布局示意图**（Width=21, Height=6）：

```
y=5 │ * * * * * * * * * * * * * * * * * * * * * │  ← Stay 天花板
y=4 │ * * * * * * * * * * * * * * * * * * * * * │  ← Stay 天花板
y=3 │ . . . . . . . . . . . . . . . . . . . . . │
y=2 │ . . . . . . . . . . . . . . . . . . . . . │
y=1 │ . . . . . . . . . . . . . . . . . . . . . │
y=0 │ > . . . . . . > . . . . . . > . . . . . . │  ← OneWay 巡逻点
     x=0       x=7       x=14       x=21
```

**运动行为：**

- 天花板（Stay）：完全静止，封锁顶部区域
- 地面巡逻点（OneWay）：每 1.5 秒向右移动 1 格，到达右边界回绕
- 每个巡逻点**独立**是一个 `Game01_Ledone` 对象（单点），各自循环平移
- 玩家需要在中间层活动，同时注意底部移动的巡逻点

---

## 七、Maze05 — 柱阵 + 右侧巡逻

**代码位置**：`Game_Map01.cs` → `Initmap_Maze_05()`（第 333~366 行）

### 全局参数

| 参数 | 值 |
|---|---|
| `max_runtime` | **1.5 秒** |

### 激光布局（无 Jieduan_Now 分支）

| 编号 | 坐标范围 | 运动类型 | 说明 |
|---|---|---|---|
| 1 | 底部柱阵：x%8==0, y∈[0,2] | **Stay** | 地面固定柱子 |
| 2 | 右上角 3x2：x∈[Width-3, Width-1], y∈[Height-2, Height-1] | **OneWay** | 右上角平移方块 |

**柱阵点位计算**：

每隔 8 列放一根 3 格高的柱子（x=0, 8, 16, ...），每根柱子 y∈[0,2]（3 个点）。

**布局示意图**（Width=24, Height=6）：

```
y=5 │ . . . . . . . . . . . . . . . . . . . . . > > > │
y=4 │ . . . . . . . . . . . . . . . . . . . . . > > > │  ← OneWay 方块
y=3 │ . . . . . . . . . . . . . . . . . . . . . . . . │
y=2 │ * . . . . . . . * . . . . . . . * . . . . . . . │
y=1 │ * . . . . . . . * . . . . . . . * . . . . . . . │  ← Stay 柱阵
y=0 │ * . . . . . . . * . . . . . . . * . . . . . . . │
     x=0       x=8       x=16              x=21-23
```

**运动行为：**

- 柱阵（Stay）：固定不动，形成底部障碍柱
- 右上方块（OneWay）：3x2 方块向右平移，1.5 秒一步，右边界回绕
- 组合效果：底部有柱子限制移动，顶部有移动方块扫过

---

## 八、Maze06 — 高速列墙

**代码位置**：`Game_Map01.cs` → `Initmap_Maze_06()`（第 367~385 行）

### 全局参数

| 参数 | 值 | 备注 |
|---|---|---|
| `max_runtime` | **0.5 秒** | 初始设为 1.5，创建对象后覆盖为 **0.5** |

> **注意**：这是所有 Maze 中 **最快** 的节拍（0.5 秒/步），移动速度是默认 1.5 秒的 **3 倍**。

### 激光布局（无 Jieduan_Now 分支）

仅创建 **1 个激光对象**：

| 编号 | 坐标范围 | 运动类型 | 说明 |
|---|---|---|---|
| 1 | 全高度列墙：x%8==0, y∈[0, Height-1] | **OneWay** | 贯穿全场的列墙 |

**列墙点位计算**：

每隔 8 列放一面全高度的墙（x=0, 8, 16, ...），每面墙 y∈[0, Height-1]。

**布局示意图**（Width=24, Height=6）：

```
y=5 │ * . . . . . . . * . . . . . . . * . . . . . . . │
y=4 │ * . . . . . . . * . . . . . . . * . . . . . . . │
y=3 │ * . . . . . . . * . . . . . . . * . . . . . . . │  ← 全高度列墙
y=2 │ * . . . . . . . * . . . . . . . * . . . . . . . │
y=1 │ * . . . . . . . * . . . . . . . * . . . . . . . │
y=0 │ * . . . . . . . * . . . . . . . * . . . . . . . │
     x=0       x=8       x=16
```

**运动行为：**

- 所有列墙作为 **一个 pos_group**，OneWay 统一向右平移
- 节拍 0.5 秒 → 每 0.5 秒所有墙体右移 1 格
- 墙体到达右边界后回绕到 x=0
- 玩家需要跟着墙的间隙一起移动，否则会被追上

**难度特点**：

- 仅 1 个对象但覆盖范围大（全高度列墙）
- 高速移动（0.5s/步 vs 其他 1.5s/步），节奏紧迫
- 玩家必须持续移动，不能停留

---

## 九、Maze07 — 天花板 + 中心散射

**代码位置**：`Game_Map01.cs` → `Initmap_Maze_07()`（第 386~412 行）

### 全局参数

| 参数 | 值 | 备注 |
|---|---|---|
| `max_runtime` | **1.0 秒** | 初始设为 1.5，创建后覆盖为 **1.0** |

### 激光布局（无 Jieduan_Now 分支）

| 编号 | 坐标范围 | 运动类型 | 说明 |
|---|---|---|---|
| 1 | 全宽顶部 3 行：x∈[0, Width-1], y∈[Height-3, Height-1] | **Stay** | 天花板固定障碍（`donw_Y = Height-3`） |
| 2~9 | 从中心向两侧辐射的 8 个单点 | **OneWay** | 地面散射巡逻 |

**散射单点的初始坐标**（i=0,1,2,3）：

| i | 左侧点 (x, y=0) | 右侧点 (x, y=0) |
|---|---|---|
| 0 | (Width/2, 0) | (Width/2, 0) — 重合 |
| 1 | (Width/2 - 2, 0) | (Width/2 + 2, 0) |
| 2 | (Width/2 - 4, 0) | (Width/2 + 4, 0) |
| 3 | (Width/2 - 6, 0) | (Width/2 + 6, 0) |

> 注意：i=0 时左右点重合（都是 Width/2），实际有效散射点为 **7 个**（1 个中心 + 左 3 + 右 3）。

**布局示意图**（Width=20, Height=6）：

```
y=5 │ * * * * * * * * * * * * * * * * * * * * │
y=4 │ * * * * * * * * * * * * * * * * * * * * │  ← Stay 天花板（3行）
y=3 │ * * * * * * * * * * * * * * * * * * * * │
y=2 │ . . . . . . . . . . . . . . . . . . . . │
y=1 │ . . . . . . . . . . . . . . . . . . . . │
y=0 │ . . . . > . > . > . > . > . > . > . . . │  ← 8个 OneWay 散射点
     x=0    x=4  x=6  x=8 x=10 x=12 x=14 x=16
```

**运动行为：**

- 天花板（Stay）：固定，封锁顶部 3 行。`donw_Y` 设为 `Height-3`，但对 Stay 类型无影响
- 散射点（OneWay）：每 1.0 秒各自向右移动 1 格，到达边界回绕
- 每个散射点**独立**（各自一个 `Game01_Ledone` 对象）
- 8 个点同步移动，形成一排巡逻线向右推进

---

## 十、Maze08 — 三段漂移 + 地柱

**代码位置**：`Game_Map01.cs` → `Initmap_Maze_08()`（第 438~462 行）

### 全局参数

| 参数 | 值 |
|---|---|
| `max_runtime` | **1.5 秒** |

### 激光布局（无 Jieduan_Now 分支）

| 编号 | 坐标范围 | 运动类型 | 说明 |
|---|---|---|---|
| 1 | 三个 3x3 方块（顶部区域） | **OneWay** | 顶部三段漂移方块 |
| 2~4 | 三个单点（底部） | **Stay** | 地面固定柱 |

**对象 1 — 三段漂移方块的坐标构成：**

三个 3x3 方块合并为一个 `pos_group`（共 27 个点）：

| 段（n） | x 范围 | y 范围 |
|---|---|---|
| n=0 | [0, 2] | [Height-3, Height-1] |
| n=1 | [8, 10] | [Height-3, Height-1] |
| n=2 | [16, 18] | [Height-3, Height-1] |

**对象 2~4 — 地面柱：**

| 编号 | 坐标 |
|---|---|
| 2 | (0, 0) |
| 3 | (8, 0) |
| 4 | (16, 0) |

**布局示意图**（Width=24, Height=6）：

```
y=5 │ > > > . . . . . > > > . . . . . > > > . . . . . │
y=4 │ > > > . . . . . > > > . . . . . > > > . . . . . │  ← OneWay 漂移（合为一组）
y=3 │ > > > . . . . . > > > . . . . . > > > . . . . . │
y=2 │ . . . . . . . . . . . . . . . . . . . . . . . . │
y=1 │ . . . . . . . . . . . . . . . . . . . . . . . . │
y=0 │ * . . . . . . . * . . . . . . . * . . . . . . . │  ← Stay 地柱
     x=0       x=8       x=16
```

**运动行为：**

- 三个顶部方块作为 **同一个 `pos_group`**，OneWay 统一向右平移
- 每 1.5 秒整体右移 1 格，到达边界回绕
- 三个方块始终保持 8 格间距（因为是整体移动）
- 地面柱（Stay）固定不动，限制玩家底部移动
- 组合效果：顶部有三段移动方块扫过，底部有固定柱子，中间层相对安全但空间有限

---

## 十一、Maze09 — 闪烁围栏

**代码位置**：`Game_Map01.cs` → `Initmap_Maze_09()`（第 413~437 行）

### 全局参数

| 参数 | 值 | 备注 |
|---|---|---|
| `max_runtime` | **2.0 秒** | 初始设为 1.5，创建后覆盖为 **2.0** |

> 这是所有 Maze 中 **最慢** 的节拍（2.0 秒/步），但闪烁效果使覆盖面积很大。

### 激光布局（无 Jieduan_Now 分支）

仅创建 **1 个激光对象**，包含大面积坐标：

**pos_group 构成（合并为一组）：**

1. **顶部 3 行全宽横条**：x∈[0, Width-1], y∈[Height-3, Height-1]
2. **左侧全高竖条**：x=0, y∈[0, Height-1]
3. **中间全高竖条**：x=Width/2, y∈[0, Height-1]
4. **右侧全高竖条**：x=Width-1, y∈[0, Height-1]

运动类型：**Shake**（闪烁）

**布局示意图**（Width=20, Height=6）：

```
y=5 │ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ │  ← 顶部横条
y=4 │ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ │  ← 顶部横条
y=3 │ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ │  ← 顶部横条
y=2 │ ~ . . . . . . . . . ~ . . . . . . . . ~ │
y=1 │ ~ . . . . . . . . . ~ . . . . . . . . ~ │  ← 三条竖线
y=0 │ ~ . . . . . . . . . ~ . . . . . . . . ~ │
     x=0              x=10              x=19

     ~ = Shake 闪烁区域（每 2 秒交替显隐）
```

**运动行为：**

- 所有点合并为一个 Shake 对象，同步闪烁
- 每 2 秒切换一次：显示 → 隐藏 → 显示 → ...
- **显示期间**：围栏全亮，覆盖面积极大，玩家被困在 2 个小格子内
- **隐藏期间**：围栏消失，玩家可以自由移动
- 安全区域被分割为 **2 个独立区间**：
  - 区间 A：x∈[1, Width/2-1], y∈[0, Height-4]
  - 区间 B：x∈[Width/2+1, Width-2], y∈[0, Height-4]

**难度特点**：

- 覆盖面积最大的单个对象
- 闪烁机制创造 "开窗期" — 玩家需在围栏隐藏时快速移动
- 节拍最慢（2 秒），给玩家思考和移动的窗口

---

## 十二、参数速查表

### 各 Maze 核心参数对比

| Maze | max_runtime | 对象数量 | 运动类型组合 | 难度特征 |
|---|---|---|---|---|
| **00** | 1.5s | 5 | Stay x5 | 纯静态散点，入门级 |
| **01** | 1.5s | 4 | Stay x4 | 静态方块 + 地面点 |
| **02** | 1.5s | 4 | OneWay x2 + Stay x2 | 方块平移 + 静态点 |
| **03** | 1.5s | 2 | Stay x1 + Shake x1 | 大面积遮挡 + 角落闪烁 |
| **04** | 1.5s | N+1 | Stay x1 + OneWay xN | 天花板封锁 + 地面巡逻 |
| **05** | 1.5s | 2 | Stay x1 + OneWay x1 | 柱阵障碍 + 方块巡逻 |
| **06** | **0.5s** | 1 | OneWay x1 | 高速列墙，最快节拍 |
| **07** | **1.0s** | 9 | Stay x1 + OneWay x8 | 天花板 + 散射巡逻 |
| **08** | 1.5s | 4 | OneWay x1 + Stay x3 | 三段漂移 + 地柱 |
| **09** | **2.0s** | 1 | Shake x1 | 大围栏闪烁，最慢节拍 |

### 运动类型使用统计

| 运动类型 | 使用的 Maze |
|---|---|
| **Stay** | 00, 01, 03, 04, 05, 07, 08 |
| **Shake** | 03, 09 |
| **OneWay** | 02, 04, 05, 06, 07, 08 |
| **Down/Up** | 无（Maze 层面未使用） |
| **Trun** | 无（预留未实现） |

### 速度梯度排列

```
最快                                                        最慢
  ├──────────┼──────────────┼──────────────┼──────────────┤
  0.5s       1.0s           1.5s           2.0s
  Maze06     Maze07         Maze00~05,08   Maze09
```

### MapIndex 与 Maze 的映射关系

```
MapIndex % 12:
  0  → Maze00    4  → Maze04    8  → Maze08
  1  → Maze01    5  → Maze05    9  → Maze09
  2  → Maze02    6  → Maze06   10  → (无方法，空关)
  3  → Maze03    7  → Maze07   11  → (无方法，空关)
```

---

> **文档版本**：v1.0
> **生成日期**：2026-02-08
> **对应代码**：`Game_Map01.cs`, `Game01_Ledone.cs`
