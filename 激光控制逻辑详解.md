# 激光控制逻辑详解

> 本文档详细记录了项目中 **所有游戏模式** 的激光（LED）控制逻辑，包括控制参数、代码位置、运动方式和碰撞检测。

---

## 目录

- [一、系统总架构](#一系统总架构)
- [二、底层渲染管线](#二底层渲染管线)
- [三、Game01 — 镭射闯关模式（LeiSheWu）](#三game01--镭射闯关模式leishe)
  - [3.1 游戏流程与阶段](#31-游戏流程与阶段)
  - [3.2 地面激光矩阵](#32-地面激光矩阵)
  - [3.3 移动激光障碍物 (Game01_Ledone)](#33-移动激光障碍物-game01_ledone)
  - [3.4 迷宫模式 (Initmap_Maze)](#34-迷宫模式-initmap_maze)
  - [3.5 镭射图案系统 (GameLeiSheLedControl)](#35-镭射图案系统-gameleisledcontrol)
  - [3.6 PPD 目标按钮](#36-ppd-目标按钮)
  - [3.7 终点按钮](#37-终点按钮)
  - [3.8 保护时间机制](#38-保护时间机制)
- [四、Game00 — 基础点阵模式](#四game00--基础点阵模式)
  - [4.1 游戏流程](#41-游戏流程)
  - [4.2 LED 点阵控制](#42-led-点阵控制)
  - [4.3 墙灯系统 (Map_WallLED)](#43-墙灯系统-map_wallled)
  - [4.4 动画结构体 (LedAnim_Struts)](#44-动画结构体-ledanim_struts)
- [五、Game02 — 扩展点阵模式](#五game02--扩展点阵模式)
- [六、Game03 — 颜色匹配模式](#六game03--颜色匹配模式)
- [七、Game04 — 魔眼模式](#七game04--魔眼模式)
- [八、Game05 — 设备检测模式](#八game05--设备检测模式)
- [九、Game06 — 炸弹模式](#九game06--炸弹模式)
- [十、Game07 — 电路模式](#十game07--电路模式)
- [十一、LED 动画系统 (LedAnim)](#十一led-动画系统-ledanim)
- [十二、关卡配置系统](#十二关卡配置系统)
- [十三、全局速度公式汇总](#十三全局速度公式汇总)

---

## 一、系统总架构

整个项目包含 **两大类激光系统**：

| 系统类型 | 适用游戏 | 核心控制类 | 渲染方式 |
|---|---|---|---|
| **镭射激光系统 (LeiShe)** | Game01 | `GameLeiSheLedControl`, `GameLeiSheBase` | `Framebuffer.Update_TransmitLedColor()` |
| **LED 点阵系统** | Game00, Game02~07 | `GameLedControl` | `Framebuffer.Update_PointColor()`, `DrawPic` |

两种系统最终都通过 `Framebuffer` 层统一发送到硬件。

---

## 二、底层渲染管线

### 2.1 Framebuffer — 硬件通信中枢

- **文件**: `Assets/Scripts/Fun/Framebuffer.cs`
- **核心方法**:

| 方法 | 用途 |
|---|---|
| `Update_TransmitLedColor(int id, uint color, enPointSta sta)` | 按 ID 更新发射端 LED 颜色 |
| `Update_TransmitLedColor(int x, int y, uint color, enPointSta sta)` | 按坐标更新发射端 LED 颜色 |
| `Update_TransmitLedClearAll()` | 清除所有发射端 LED |
| `Update_PointColor(int id, uint color, enPointSta statue)` | 按 ID 更新 LED 颜色 |
| `Update_PointColor(int x, int y, uint color, enPointSta statue)` | 按坐标更新 LED 颜色 |
| `Update_ColorFull(uint color, enPointSta statue)` | 全部 LED 设为统一颜色 |
| `Update_TargetLedColor(int id, uint color)` | 更新目标 LED 颜色 |
| `Update_TargetLedColor(int ch, int id, uint color)` | 按通道更新目标 LED |
| `Update_TargetLedColorAll(int ch, uint color)` | 通道内全部目标 LED |
| `MapBuffer()` | 批量发送到硬件（`CmdIO_YDGZ.CMD0_SendCmd_LedOne()`） |

### 2.2 DrawPic — 图形绘制工具

- **文件**: `Assets/Scripts/Fun/DrawPic.cs`
- **核心方法**:

| 方法 | 用途 |
|---|---|
| `DrawRol(x, y, length, color, sta)` | 画水平行 |
| `DrawCol(x, y, length, color, sta)` | 画垂直列 |
| `DrawRectangle(...)` | 画矩形 |
| `DrawCirculation(...)` | 画圆 |
| `DrawPointId(id, color, sta)` | 画单个点 |
| `DrawPoint(x, y, color, sta)` | 按坐标画点 |

### 2.3 LaserController — 激光矩阵控制器

- **文件**: `Assets/Scripts/Game/LaserPPD/LaserController.cs`
- **接口**: `ILaserController`（`Assets/Scripts/Game/LaserPPD/ILaserController.cs`）
- **功能**: 将二维矩阵 `int[,] laserMatrix` 转换为 S 形扫描索引数组后发送到硬件
- **核心方法**:
  - `InitMatrix(int width, int height)` — 初始化激光矩阵
  - `SendMatrix(int[,] targetMatrix)` — 发送矩阵数据
  - `MatrixToIndexArray(int[,] matrix)` — 矩阵转 S 形索引
  - `SendLaserData(int laserId, bool isOpen, uint[] data, int length)` — 发送激光数据

---

## 三、Game01 — 镭射闯关模式（LeiShe）

### 3.1 游戏流程与阶段

- **主控文件**: `Assets/Scripts/Game/Game01/Game01_Main.cs`
- **玩家控制**: `Assets/Scripts/Game/Game01/Game01_Player.cs`
- **状态枚举**: `en_Game01_Sta`

每一关分为 **4 个阶段** 循环（`currJieDuan` 0→3）：

| 阶段 | currJieDuan | 类型 | 描述 |
|---|---|---|---|
| 阶段 0 | 0 | PPD 目标按钮 | 在 ch=4 通道上随机生成目标，玩家需要按对应按钮；同时**地面激光全场覆盖** |
| 阶段 1 | 1 | 移动激光障碍 | `Game01_Ledone` 激光障碍物活跃，玩家需到达终点按钮 |
| 阶段 2 | 2 | PPD 目标按钮 | 在 ch=5 通道上随机生成目标；同时**地面激光全场覆盖** |
| 阶段 3 | 3 | 移动激光障碍 | `Game01_Ledone` 激光障碍物活跃，玩家需到达终点按钮；完成后过关 |

**过关条件**:
- 阶段 0/2: 按完所有目标 (`tarageNum <= 0`)
- 阶段 1: 按下终点按钮 (`targetKeyId = ChannelLength[0..4] - 1`)
- 阶段 3: 按下终点按钮 (`targetKeyId = ChannelLength[0..4] - 2`)
- 完成全部 4 个阶段后进入 `en_Player01Sta.Pass`

**阶段切换代码位置**: `Game01_Player.cs` 第246-354行 `Update()` 中的 `switch (currJieDuan)`

---

### 3.2 地面激光矩阵

- **文件**: `Assets/Scripts/Game/Game_Map/Game_Map01.cs` — `Update()` 方法（第465-516行）
- **生效阶段**: `currJieDuan == 0` 或 `currJieDuan == 2`

**控制逻辑**:
```csharp
// Game_Map01.cs 第475-506行
if (Game01_Main.instance.player.currJieDuan == 0 || Game01_Main.instance.player.currJieDuan == 2)
{
    for (int i = 0; i < Set.setVal.Width; i++)
    {
        for (int k = 0; k < Set.setVal.Height; k++)
        {
            // 全场亮灯，颜色值 255
            Framebuffer.Update_TransmitLedColor(i, k, 255, enPointSta.Target);
            // 仅在第 0 列检测碰撞（地面传感器）
            if (i == 0)
            {
                if (LedKey.KeyStatus(startId + Framebuffer.MappingId(i, k)) == false)
                {
                    if (protectTime <= 0)
                    {
                        MusicManager.instance.Play_Fails();
                        protectTime = 1;  // 扣血后 1 秒保护
                        FjData.g_Fj[0].Life--;
                    }
                }
            }
        }
    }
}
```

**控制参数**:

| 参数 | 位置 | 默认值 | 说明 |
|---|---|---|---|
| `protectTime` | Game_Map01.cs 第20行 | `5f` | 全局无敌时间（秒） |
| 清屏间隔 | Game_Map01.cs 第508-513行 | `0.02f` | 每 0.02 秒执行一次 `Framebuffer.Update_TransmitLedClearAll()` |

---

### 3.3 移动激光障碍物 (Game01_Ledone)

- **文件**: `Assets/Scripts/Game/Game_Map/Game01_Ledone.cs`（共 244 行）
- **生效阶段**: `currJieDuan == 1` 或 `currJieDuan == 3`

**生效条件** (Update 第237-238行):
```csharp
if (Game01_Main.instance.player.currJieDuan != 1 && Game01_Main.instance.player.currJieDuan != 3)
    return;
```

#### 3.3.1 移动类型枚举

```csharp
// Game01_Ledone.cs 第18-26行
public enum en_Move_Type
{
    Stay,    // 静止不动
    Shake,   // 闪烁（按间隔切换可见/隐藏）
    OneWay,  // 水平单向移动（到边界后环绕）
    Down,    // 垂直往返移动（下行优先）
    Up,      // 垂直往返移动（上行优先）
    Trun,    // 旋转（占位，未实现）
}
```

#### 3.3.2 各移动类型详细逻辑

**Stay** — 静止不动
- 不进入 Moving() 的 switch 分支
- 激光点固定在 `pos_group` 中的初始坐标
- 每帧仅执行渲染和碰撞检测

**Shake** — 闪烁效果
- 代码位置: Moving() 第136-138行
- 逻辑: 每隔 `max_runtime` 秒切换 `isShaking` 布尔值
- 当 `isShaking == false` 时跳过渲染和碰撞检测，实现闪烁

**OneWay** — 水平移动（环绕）
- 代码位置: Moving() 第140-165行
- `dir == 0`: 所有点 x 坐标 +1（向右），到达 `Width` 后回绕到 0
- `dir == 1`: 所有点 x 坐标 -1（向左），到达 0 后回绕到 `Width-1`
- 整组 `pos_group` 坐标同步更新

**Up** — 垂直往返（上行优先）
- 代码位置: Moving() 第83-108行
- `dir == 0`: y 坐标递增（向上），到达 `Height` 后反转方向
- `dir == 1`: y 坐标递减（向下），到达 `donw_Y` 后反转方向
- 每次移动后 `stayTime = 1.0f`（停顿 1 秒）

**Down** — 垂直往返（下行优先）
- 代码位置: Moving() 第110-135行
- 逻辑与 Up 相同，但 `dir` 初始方向不同
- 每次移动后 `stayTime = 1.0f`

**Trun** — 旋转
- 代码位置: Moving() 第167-169行
- 当前为空实现（占位）

#### 3.3.3 移动节拍控制

```csharp
// Game01_Ledone.cs 第70-78行
runtime += Time.deltaTime;
if (stayTime > 0)
{
    stayTime -= Time.deltaTime;  // 停顿中，不执行移动
    return;
}
if (runtime > Game_Map01.instance.max_runtime)  // 达到移动间隔
{
    Game_Map01.instance.protectTime = 0.4f;  // 移动时给短暂保护
    runtime = 0;
    // ... 执行移动 ...
}
```

**核心参数**:

| 参数 | 所在对象 | 说明 |
|---|---|---|
| `Game_Map01.instance.max_runtime` | Game_Map01 | 全局移动节拍间隔（秒），越小越快 |
| `runtime` | 每个 Game01_Ledone | 当前已累计时间 |
| `stayTime` | 每个 Game01_Ledone | 停顿剩余时间（Up/Down 设为 1.0f） |
| `MaxstayTime` | 每个 Game01_Ledone | 最大停顿时间 |
| `dir` | 每个 Game01_Ledone | 移动方向（0/1） |
| `donw_Y` | 每个 Game01_Ledone | 垂直移动下界 |
| `pos_group` | 每个 Game01_Ledone | 所有坐标点列表（List<Vector2Int>） |
| `isShaking` | 每个 Game01_Ledone | Shake 类型的可见状态 |

#### 3.3.4 碰撞检测（不可修改区域）

```csharp
// Game01_Ledone.cs 第196-221行
Framebuffer.Update_TransmitLedColor(pos_group[i].x, pos_group[i].y, 255, enPointSta.Target);
if (LedKey.KeyStatus(...) == false && Game01_Main.instance.statue == en_Game01_Sta.Play)
{
    if (Game_Map01.instance.protectTime <= 0)
    {
        if (FjData.g_Fj[0].Life > 0)
        {
            FjData.g_Fj[0].Life--;                      // ⚠️ 不可修改
            Game_Map01.instance.protectTime = 3f;         // 扣血后 3 秒保护
            GameLeiSheBase.gamePoint[i].bindCnt = 4;
            MusicManager.instance.Play_Fails();
        }
    }
}
```

---

### 3.4 迷宫模式 (Initmap_Maze)

- **文件**: `Assets/Scripts/Game/Game_Map/Game_Map01.cs`
- **选择逻辑**: `Initmap()` 第36行 `Invoke("Initmap_Maze_" + (Main.MapIndex % 12).ToString("D2"), 0)`

#### 各迷宫模式配置一览

| 迷宫 | max_runtime | 激光分布 | 移动类型 | 阶段变化 |
|---|---|---|---|---|
| **Maze_00** | 1.5f | 5个散布静态点 | Stay | case 1: 2组 3x2 OneWay; case 2: 中心区域静态; case 3: 全场+边角 Shake |
| **Maze_01** | 1.5f | 2组 3x2 静态块 + 2个点 | Stay | case 1: 追加 OneWay 块; case 2: 中心静态; case 3: 全场+边角 Shake |
| **Maze_02** | 1.5f | 2组 3x2 OneWay + 2点 | OneWay | case 2: 中心静态; case 3: 全场+边角 Shake |
| **Maze_03** | 1.5f | 全场覆盖 + 边角 Shake | Stay + Shake | 无阶段变化（固定布局） |
| **Maze_04** | 1.5f | 全宽顶排 2 行 Stay | Stay + OneWay | 每隔 7 列一个 OneWay 点 |
| **Maze_05** | 1.5f | 每隔 8 列柱体 | Stay + OneWay | 右上角 3x2 区域 OneWay |
| **Maze_06** | **0.5f** | 每隔 8 列全高墙 | OneWay | 初始 1.5f 后覆盖为 0.5f（最快模式） |
| **Maze_07** | **1.0f** | 顶排 3 行 Stay + 对称 OneWay | Stay + OneWay | 8个对称 OneWay 点 |
| **Maze_08** | 1.5f | 3 组 3x3 块 + 3 个静态点 | OneWay + Stay | 块间距 8 列 |
| **Maze_09** | **2.0f** | 顶排+两侧+中线 | Shake | 全部 Shake（最慢模式） |

#### 迷宫中的 `New_LedOne` 调用方式

```csharp
// 单点生成（x, y, dir, moveType）
New_LedOne(5, 0, 0, Game01_Ledone.en_Move_Type.Stay);

// 多点组生成（坐标列表, dir, moveType）
List<Vector2Int> pos = new List<Vector2Int>();
for (int i = 0; i < 3; i++)
    for (int k = 0; k < 2; k++)
        pos.Add(new Vector2Int(i, Set.setVal.Height - 1 - k));
New_LedOne(pos, 0, Game01_Ledone.en_Move_Type.OneWay);
```

---

### 3.5 镭射图案系统 (GameLeiSheLedControl)

- **文件**: `Assets/Scripts/Game/Game01/GameLeiSheLedControl.cs`
- **基类**: `Assets/Scripts/Game/Game01/GameLeiSheBase.cs`

#### 核心常量

```csharp
// GameLeiSheLedControl.cs 第141-144行
public const uint LEISHE_COLOR = 63;  // 硬件激光颜色值
```

#### 核心方法

| 方法 | 说明 |
|---|---|
| `LedInit(GameLevelSetting)` | 根据关卡配置初始化图案和动画 |
| `RunStart()` | 启动动画循环 |
| `Run()` | 驱动动画播放 |
| `Pic_Init(PresetPic)` | 加载预设图案 |
| `Pic_Draw()` | 渲染图案到 Framebuffer |
| `GetPicPointValue(x, y)` | 获取指定坐标的图案值 |
| `LedCoordinate()` | 建立坐标映射字典 |

#### PrePic_Copy 数组

`PrePic_Copy[]` 是一个静态 int 数组，标记每个 LED 位置是否有激光图案：
- `PrePic_Copy[i] != 0` → 有激光，渲染 `LEISHE_COLOR`
- `PrePic_Copy[i] == 0` → 无激光，渲染黑色

```csharp
// Pic_Draw() — GameLeiSheLedControl.cs 第175-198行
for (int i = 0; i < ledLen; i++)
{
    if (PrePic_Copy[i] != 0)
    {
        Framebuffer.Update_TransmitLedColor(i, LEISHE_COLOR, enPointSta.Target);
    }
    else
    {
        Framebuffer.Update_TransmitLedColor(i, 0, enPointSta.None);
    }
}
```

#### GameLeiSheBase — 激光点状态管理

- `gamePoint[]` — 全局激光点状态数组（颜色、闪烁计数 `bindCnt`）
- `targetLed[]` — 壁灯 LED 数组
- `tab_Point[]` — 坐标映射表
- `Update_PointColor(x, y, color, sta)` — 更新单点颜色
- `Update_ColorFull(color, sta)` — 全场颜色
- `Check()` — 闪烁效果处理（`bindCnt` 递减）

---

### 3.6 PPD 目标按钮

- **文件**: `Assets/Scripts/Game/Game01/Game01_Player.cs`
- **生效阶段**: `currJieDuan == 0`（ch=4）和 `currJieDuan == 2`（ch=5）

#### 目标生成 — `GetRandom_Tarage(int ch, int targetNum)`

```csharp
// Game01_Player.cs 第654-681行
void GetRandom_Tarage(int ch, int targetNum)
{
    int cnt = 300;  // 最大尝试次数，防止死循环
    while (targetNum > 0 && cnt > 0)
    {
        cnt--;
        randomX = Random.Range(0, Set.setVal.PPDWidth);
        randomY = Random.Range(0, Set.setVal.PPDHeight);
        int id = randomX + randomY * Set.setVal.PPDWidth;
        // ... 计算通道偏移 ...
        if (GameLedControl.gamePoint[id].statue == enPointSta.None)
        {
            GameLedControl.gamePoint[id].statue = enPointSta.Target;
            Framebuffer.Update_TargetLedColor(ch, pointId, 0x0000ff);  // 蓝色目标
            tarageNum++;
            targetNum--;
        }
    }
}
```

**调用位置**: `ChangeStatue(en_Player01Sta.Play)` 中（第504-506行）
- `GetRandom_Tarage(4, 10)` — 阶段 0，在 ch4 上生成 10 个目标
- `GetRandom_Tarage(5, 10)` — 阶段 2，在 ch5 上生成 10 个目标

#### 非目标填充 — `GetOtherLed(int ch)`

```csharp
// Game01_Player.cs 第703-724行
// 将所有非 Target 点设为 Die（红色 0xff0000）
if (GameLedControl.gamePoint[picId].statue != enPointSta.Target)
{
    GameLedControl.gamePoint[picId].statue = enPointSta.Die;
    Framebuffer.Update_TargetLedColor(ch, pointId, 0xff0000);
}
```

#### 按键检测 — `CheckLedKey(int ch)`

```csharp
// Game01_Player.cs 第606-653行
// 按中 Target: 加 10 分，tarageNum--
// 按中 Die: 扣 5 分（errorCD 冷却 1 秒），不扣血
```

---

### 3.7 终点按钮

- **文件**: `Assets/Scripts/Game/Game01/Game01_Player.cs` 第278-333行
- **阶段 1 终点**: `targetKeyId = ChannelLength[0..4] - 1`
- **阶段 3 终点**: `targetKeyId = ChannelLength[0..4] - 2`
- 按下即通过当前阶段，播放正确音效

**LED 闪烁**: `TargetLed_Run()` 方法（第1100-1116行），每 0.5 秒切换终点按钮 LED 亮灭

---

### 3.8 保护时间机制

Game01 有两层保护时间：

#### 全局保护 — `Game_Map01.protectTime`

| 触发时机 | 设置值 | 代码位置 |
|---|---|---|
| 地图初始化 | 5 秒 | Game_Map01.cs 第24行 |
| Ledone 对象创建 | 4 秒 | Game01_Ledone.cs 第31、46行 |
| Ledone 每次移动 | 0.4 秒 | Game01_Ledone.cs 第79行 |
| 地面碰撞扣血后 | 1 秒 | Game_Map01.cs 第491行 |
| Ledone 碰撞扣血后 | 3 秒 | Game01_Ledone.cs 第214行 |
| 阶段切换时 | 3 秒 | Game01_Player.cs 第273、284、317、328行 |

#### 每LED独立保护 — `Game01_Player.protectTime[i]`

- **文件**: `Game01_Player.cs` 第907行
- **公式**: `protectTime[JieShou_ID] = 2f - 0.02f * Set.setVal.LeiShe_LMD`
- `LeiShe_LMD` 越大 → 保护时间越短 → 灵敏度越高
- 用于 `CheckRxKey()` 中逐点检测（第908-990行）

---

## 四、Game00 — 基础点阵模式

### 4.1 游戏流程

- **主控文件**: `Assets/Scripts/Game/Game00/Game00_Main.cs`
- **地图文件**: `Assets/Scripts/Game/Game_Map/Game_Map00.cs`
- **玩家文件**: `Assets/Scripts/Game/Game00/Game00_Player.cs`
- **状态枚举**: `en_Game00_Sta`

**阶段系统**:
- `Index_JieDuan` 从 0 开始，每完成一个阶段递增（`NextJieDuan()`）
- 最大阶段数 `MaxJieDuan` 由关卡决定（默认 2，部分关卡 3~5）
- 全部阶段完成后游戏结束

### 4.2 LED 点阵控制

#### 速度公式 — `Get_NowTarage()`

```csharp
// Game_Map00.cs Get_NowTarage()
// 基础公式：
MaxrunTime = 0.5f - 0.03f * Game00_Main.instance.Index_JieDuan;

// 用户设置覆盖：
MaxrunTime = 0.35f - 0.05f * SettingInGame_01.instance.GetMoveSpeed(Index_JieDuan);

// 最低阈值：
if (MaxrunTime <= 0.05f) MaxrunTime = 0.05f;
```

#### LED 点状态类型

| 状态 | 枚举值 | 颜色 | 说明 |
|---|---|---|---|
| `None` | 无 | 黑色 | 空白 |
| `Target` | 目标 | 蓝色 (0x0000ff) | 玩家需要触碰 |
| `Die` | 危险 | 红色 (0xff0000) | 触碰扣血扣分 |
| `Rest` | 安全 | 绿色 (0x00ff00) | 安全区域/路径 |

#### 碰撞检测 — `CheckLedKey()`

```csharp
// Game00_Main.cs 第1353-1517行
// 触碰 Target: 加分 + RemainPoint-- + SafeTime 重置为 10
// 触碰 Die: 扣血 + 扣分 + errorCD 冷却 1 秒
```

#### 安全时间 — `SafeTime`

- **声明**: `public float SafeTime = 10f`
- **逻辑**: 当剩余目标 `TarageNum_now <= 3` 时开始倒计时
- **到期**: `SafeTime <= 0` → `isClearAll = true`（自动通过当前阶段）
- **重置**: 每次命中目标时重置为 10

#### 关卡运行方法示例

**Run_Map01** — 河流路径:
```csharp
void Run_Map01()
{
    runTime -= Time.deltaTime;
    if (runTime <= 0)
    {
        runTime = MaxrunTime;
        NextRiver_Pos();  // 更新河流路径位置
    }
}
```

**Run_Map02** — 移动方块:
```csharp
void Run_Map02()
{
    runTime -= Time.deltaTime;
    if (runTime <= 0)
    {
        runTime = MaxrunTime;
        ClearMap();
        ledSturts_Group[0].FangKuaiMoving();  // 方块移动
    }
}
```

**Run_Map09** — 贪吃蛇:
```csharp
void Run_Map09(int JieDuan)
{
    runTime -= Time.deltaTime;
    ledSturts_Group[0].UpdateSnake();
    if (runTime <= 0)
    {
        runTime = MaxrunTime;
        ClearMap();
        ledSturts_Group[0].MoveSnakePos();
        snakeLength = 5 + JieDuan;  // 蛇身随阶段增长
    }
}
```

---

### 4.3 墙灯系统 (Map_WallLED)

- **文件**: `Assets/Scripts/Game/Game_Map/Map_WallLED.cs`（5000+ 行）
- **功能**: 控制墙壁上的额外 LED 灯效果

#### 速度公式

```csharp
MaxrunTime = 0.35f - 0.05f * SettingInGame_01.instance.GetMoveSpeed(Index_JieDuan);
if (MaxrunTime <= 0.05f) MaxrunTime = 0.05f;
```

#### 墙灯渲染

```csharp
// Update() 中根据 targetPos[] 渲染
if (randType == 0)
    DrawPic.DrawPoint(x, y, 0xff0000, enPointSta.Die);   // 危险灯（红色）
else
    DrawPic.DrawPoint(x, y, 255, enPointSta.Target);      // 目标灯
```

---

### 4.4 动画结构体 (LedAnim_Struts)

- **文件**: `Assets/Scripts/Fun/LedAnim_Struts.cs`
- **功能**: 为 Game00/02 提供移动障碍物（方块、坦克、蛇等）

| 方法 | 功能 | 移动逻辑 |
|---|---|---|
| `UpdatePic_FangKuang(pos, width)` | 绘制矩形边框 | 在 `pos ± width` 画水平/垂直线 |
| `FangKuaiMoving()` | 方块移动 | `StartPos ± (1,0)` 或 `± (0,1)`，支持 8 方向 |
| `Moving_Tank()` | 坦克移动 | `StartPos` 按方向递增，碰边界返回 false |
| `UpdateSnake()` | 贪吃蛇渲染 | 遍历 `SnakePos[]` 绘制蛇身 |
| `MoveSnakePos()` | 蛇移动 | 更新蛇头方向，蛇身跟随 |
| `UpdatePic_DuanLie()` | 断裂效果 | 分段渲染水平/垂直线 |

---

## 五、Game02 — 扩展点阵模式

- **主控文件**: `Assets/Scripts/Game/Game02/Game02_Main.cs`
- **地图文件**: `Assets/Scripts/Game/Game_Map/Game_Map02.cs`
- **玩家文件**: `Assets/Scripts/Game/Game02/Game02_Player.cs`

### 速度公式

```csharp
// Game_Map02.cs Get_NowTarage()
MaxrunTime = 0.8f - 0.05f * SettingInGame_02.instance.set_MoveSpeed[Index_JieDuan];
if (MaxrunTime <= 0.05f) MaxrunTime = 0.05f;
```

### 特有机制

- **最大阶段数**: 4（`MaxJieDuan = 4`）
- **动画组**: `LedAnim_Struts[4]` — 最多同时 4 个移动障碍物
- **目标位置记录**: `tarage_Pos[500]` 存储目标坐标
- **支持 30 个关卡**: `InitMap_00()` ~ `InitMap_29()`

### 关卡示例

**InitMap_05** — 双向移动块:
- 2 个 `ledSturts_Group` 分别向不同方向移动
- 中间一列为安全线（绿色 `Rest`）
- `MaxrunTime` 控制移动节拍

**InitMap_27** — 多阶段方块:
- 阶段 0: 2 个移动列
- 阶段 1: 4 个方块对角移动
- 阶段 2: 4 个红色方块带门

**Run_Map14** — 断裂效果:
- 使用 `UpdatePic_DuanLie()` 渲染断裂线条
- `ledSturts_Group[0].y++` 和 `ledSturts_Group[1].x++` 实现交叉移动

---

## 六、Game03 — 颜色匹配模式

- **主控文件**: `Assets/Scripts/Game/Game03/Game03_Main.cs`
- **地图文件**: `Assets/Scripts/Game/Game_Map/Game_Map03.cs`

### 特有机制

**无全局 MaxrunTime**，使用独立计时系统：

| 参数 | 类型 | 说明 |
|---|---|---|
| `tobeachtime[300]` | float[] | 每个目标点的独立冷却计时 |
| `ChangeTarage_Time` | float | 目标切换倒计时 |
| `MaxChangeTarage_Time` | float | 目标切换间隔 |
| `MaxRedPassTime` | float | 红灯通过最大时间（默认 3） |
| `MaxRedStayTime` | float | 红灯停留时间（默认 2） |
| `isMoving` | bool | 移动模式标志 |
| `MoveAllStep` | int | 移动总步数（默认 10） |

### 目标管理

```csharp
// 每帧递减每个目标的冷却时间
for (int i = 0; i < tobeachtime.Length; i++)
{
    if (tobeachtime[i] > 0)
        tobeachtime[i] -= Time.deltaTime;
}
```

### 阶段切换 — `ChangeTarage_Map()`

- 清空全场为 `Die` 状态
- 通过 `Invoke("InitMap_XX_next", 0)` 加载下一阶段的目标布局
- 支持目标颜色匹配（`Tarage_Colors[3]`、`Other_Color0/1/2[3]`）

### 移动模式 — `Update_Move_Tarage_Pos()`

- 当 `isMoving == true` 时激活
- 按 `MovePos[]` 数组中记录的路径逐步移动目标
- `MoveTime` 控制移动速度（递减到 0 后移动一步）
- 移动完成（`MoveIndex >= MoveAllStep`）后 `isMoving = false`

---

## 七、Game04 — 魔眼模式

- **主控文件**: `Assets/Scripts/Game/Game04/Game04_Main.cs`
- **地图文件**: `Assets/Scripts/Game/Game_Map/Game_Map04.cs`

### 特有机制

| 参数 | 默认值 | 说明 |
|---|---|---|
| `Eyes_NextIndex[4]` | `{0,0,0,0}` | 4 个魔眼的下一个闪缩索引 |
| `DevEyes_Index` | 0 | 当前闪缩的魔眼编号 |
| `EyesWaitTime` | 10f | 魔眼当前剩余检测时间 |
| `MaxEyesWaitTime` | 10f | 魔眼每轮最大检测时间 |
| `RedStayTime` | 4f | 红灯持续时间 |
| `MaxRedStayTime` | 4f | 红灯最大持续时间 |
| `waitCheckTime` | 0.4f | 魔眼检测准备时间 |
| `Led_List[5]` | — | LED 列表 |
| `Led_List_Color[5]` | — | LED 颜色列表 |

### 目标展示 — `Show_Next()`

```csharp
// Game_Map04.cs 第1722-1749行
void Show_Next()
{
    ShowTime = MaxShowTime;
    waitTime = 2;
    IdleTime = MaxIdleTime = 3;
    ShowIndex++;
    // 重置 LED 列表
    for (int i = 0; i < Led_List_Color.Length; i++)
    {
        Led_List_Color[i] = 0;
        Led_List[i] = 0;
    }
    MaxJieDuan = 1;
    remainPoint = 30;
    // ...
}
```

### 阶段切换

- 各 `InitMap_XX_next()` 方法设置下一轮的 LED 颜色和目标
- 如 `InitMap_00_next()`: 单色轮换（`Led_List_Color[MoveIndex] = 255`）
- 如 `InitMap_03_next()`: 双色组合（`GetTwoColor(2)`）

---

## 八、Game05 — 设备检测模式

- **主控文件**: `Assets/Scripts/Game/Game05/Game05_Main.cs`
- **地图文件**: `Assets/Scripts/Game/Game_Map/Game_Map05.cs`

### 特有机制

| 参数 | 默认值 | 说明 |
|---|---|---|
| `SafeTime` | 1.0f | 安全间隔时间 |
| `MaxRedPassTime` | 3f | 红灯通过时间 |
| `MaxRedStayTime` | (由关卡设置) | 红灯停留时间 |
| `RedStayTime` | — | 当前红灯剩余时间 |
| `Eyes_NextIndex[4]` | — | 魔眼索引 |
| `isStartChecking_DevEeys` | bool | 是否正在检测 |
| `Checking_CountTime` | float | 检测倒计时 |

### 魔眼检测 — `Checking_DevEeys()`

```csharp
// Game_Map05.cs 第275-368行
// 对 4 个检测位（Eyes_NextIndex）逐个检查
// 渲染红色 Die 点：Framebuffer.Update_PointColor(i, 0x400000, enPointSta.Die)
// 检测按键：LedKey.KeyStatus(i)
// 被触碰: 扣血 + 扣分 + 播放失败音效
// RedStayTime 到期: 重置检测状态，恢复 Rest
```

### SafeTime 机制

- 碰撞检测中：`if (FjData.g_Fj[0].Life > 0 && SafeTime < 0)` → 双倍扣血
- 命中目标后重置：`SafeTime = 0.5f`
- 每帧递减：`SafeTime -= Time.deltaTime`

### 关卡示例

- **RunMap_00**: 数字从小到大展示 + 魔眼空闲倒计时
- **RunMap_01**: 目标灯展示 + 等待全部按中
- **RunMap_06**: 带惩罚等待的目标匹配（错误等 0.5 秒后扣血扣分）

---

## 九、Game06 — 炸弹模式

- **主控文件**: `Assets/Scripts/Game/Game06/Game06_Main.cs`
- **地图文件**: `Assets/Scripts/Game/Game_Map/Game_Map06.cs`

### 速度公式

```csharp
// Game_Map06.cs Get_NowTarage()
MaxrunTime = 0.5f - 0.03f * Game06_Main.instance.Index_JieDuan;
if (MaxrunTime <= 0.05f) MaxrunTime = 0.05f;
```

### 特有机制

| 参数 | 说明 |
|---|---|
| `isDetonate` | 是否引爆 |
| `startBooming` | 爆炸进行中标志 |
| `zadashu` | 炸弹数量 |
| `zadanx[]` / `zadany[]` | 炸弹坐标数组 |
| `ShackCnt` | 闪烁倒计数 |
| `LedAnim_Struts_06[]` | Game06 专用动画结构 |

### 炸弹机制 — `Update_Zadan()`

```csharp
// Game_Map06.cs 第2582-2622行
// 在右边界中间画红色目标点
DrawPic.DrawPointId(ppId, 0xff0000, enPointSta.Target);

// 按键触发引爆
if (LedKey.KeyStatus(ppId) || Input.GetKeyDown(KeyCode.Q))
{
    zadashu = 0;
    startBooming = true;
}

// 引爆效果：清除炸弹行和列上的所有 Rest 点 → 加分
if (startBooming)
{
    for (int i = 0; i < zadanx.Length; i++)
    {
        // 清除整行 + 整列
        // 如果是 Rest 状态 → 加分 + remainPoint--
    }
}
```

### 关卡示例

**Run_Map02** — 图案记忆:
- 倒计时闪烁 (`ShackCnt--`)
- 先展示目标图案 (`GetPicType`)
- 然后展示真实图案 + 其他LED (`GetTruePicType + GetOtherLed`)
- 按正确图案加分

**Run_Map03** — 闪烁记忆:
- 整场交替亮灭 (`goDown = !goDown`)
- 记忆后找到目标

---

## 十、Game07 — 电路模式

- **主控文件**: `Assets/Scripts/Game/Game07/Game07_Main.cs`
- **地图文件**: `Assets/Scripts/Game/Game_Map/Game_Map07.cs`

### 速度公式

```csharp
// Game_Map07.cs Get_NowTarage()
MaxrunTime = 0.5f - 0.03f * Game07_Main.instance.Index_JieDuan;
if (MaxrunTime <= 0.05f) MaxrunTime = 0.05f;
```

### 特有机制

| 参数 | 说明 |
|---|---|
| `Cnt_DianZu` | 电阻计数 |
| `Cnt_DianLiu` | 电流计数（归零时过关） |
| `startBooming` | 爆炸标志 |
| `protectTime[1000]` | 每 LED 独立保护时间 |
| `LedAnim_Struts_07[]` | Game07 专用动画结构 |
| `rxKeyStartId` | 接收端按键起始 ID |

### 独立保护时间 — `protectTime[]`

```csharp
// 初始化: Game_Map07.cs 第262-265行
for (int i = 0; i < protectTime.Length; i++)
{
    protectTime[i] = 2f - 0.02f * Set.setVal.LeiShe_LMD;
}

// 使用: CheckRxKey() 第1638-1656行
protectTime[JieShou_ID] -= Time.deltaTime;
if (protectTime[JieShou_ID] <= 0)
{
    // 触发扣血
    protectTime[JieShou_ID] = 2f - 0.02f * Set.setVal.LeiShe_LMD;  // 重置
    GameLeiSheBase.gamePoint[i].bindCnt = 10;
    MusicManager.instance.Play_Fails();
}
```

### 电路通关逻辑

```csharp
// 电流计数归零时过关
Cnt_DianLiu--;
if (Cnt_DianLiu == 0 && isStart)
{
    isClearAll = true;  // 过关
}
```

### 多通道支持

Game07 支持多阶段+多通道，`Run_Map00` 展示了完整的镭射阶段流程：
- `Index_JieDuan == 0`: PPD 目标 (ch=5)
- `Index_JieDuan == 1/3`: 镭射图案 + 终点按钮
- `Index_JieDuan == 2`: PPD 目标 (ch=3)

---

## 十一、LED 动画系统 (LedAnim)

- **文件**: `Assets/Scripts/Fun/LedAnim.cs`

### 速度计算

```csharp
// 根据游戏模式计算真实速度
switch (Set.setVal.GameChoose)
{
    case 0:  // Game00
        trueSpeed = speed - Game00_Main.instance.Index_JieDuan;
        if (trueSpeed <= 1) trueSpeed = 1;
        break;
    case 2:  // Game02
        trueSpeed = speed - SettingInGame_02.instance.set_MoveSpeed[Index_JieDuan] * 5;
        if (trueSpeed <= 10) trueSpeed = 10;
        break;
    default:
        trueSpeed = speed;
        break;
}

// 到达 trueSpeed 帧时执行一步动画
if (runTime >= trueSpeed) { /* 执行移动 */ }
```

### 动画类型一览

| 枚举 | 方法 | 运动方式 |
|---|---|---|
| `LeftToRight` | `RunAnim_LeftToRight()` | x++ 直到 `endRunPos`，循环或停止 |
| `RightToLeft` | `RunAnim_RightToLeft()` | x-- 直到 `endRunPos` |
| `LRRL` | `RunAnim_LRRL()` | 左右往返 |
| `UpToDown` | `RunAnim_UpToDown()` | y++ 直到 `endRunPos` |
| `DownToUp` | `RunAnim_DownToUp()` | y-- 直到 `endRunPos` |
| `UDDU` | `RunAnim_UDDU()` | 上下往返 |
| `SmallToBig` | `RunAnim_SmallToBig()` | radio++ 扩大 |
| `BigToSmall` | `RunAnim_BigToSmall()` | radio-- 缩小 |
| `SBBS` | `RunAnim_SBBS()` | 大小往返 |
| `TurnLeft` | `RunAnim_TurnLeft()` | 逆时针旋转 |
| `TurnRight` | `RunAnim_TurnRight()` | 顺时针旋转 |
| `TurnLR` | `RunAnim_TurnLR()` | 旋转往返 |

### 颜色表

```csharp
public readonly static uint[] tab_Color = {
    0x000000,   // 黑色
    0xfc0000,   // 红色
    0x00fc00,   // 绿色
    0x0000fc,   // 蓝色
    0xfcfc00,   // 黄色
    0xfc00fc,   // 紫色
    0x00fcfc,   // 青色
    0xfcfcfc,   // 白色
};
```

---

## 十二、关卡配置系统

### GameLevelSetting — 关卡设置

- **文件**: `Assets/Scripts/Fun/GameLevelSetting.cs`

```csharp
public class GameLevelSetting
{
    public int gameTime;         // 游戏时间（秒）
    public int life;             // 生命数
    public int targetPoint;      // 目标数
    public int wallLedNum;       // 墙灯个数
    public int bkWidth;          // 边框宽度
    public int targetNewType;    // 目标点生成方式
    public int targetDieType;    // 目标点死亡方式
    public string presetPicName; // 预设图案名称
    public string animInfoName;  // 动画配置名称
    public PresetPic picSetting; // 预设图案数据
    public AnimSet animSetting;  // 动画设置数据
}
```

**Game01（镭射闯关）默认配置**:
- `gameTime = 120`
- `life = 4`
- `targetPoint = 2 + level / 2`

### SettingInGame_01 — Game00/01 游戏内设置

- **文件**: `Assets/Scripts/Game/GameSetting/SettingInGame_01.cs`
- **支持 2 个阶段速度设置**: `set_MoveSpeed[2]`（默认 `{1,2,...,9}`）
- `GetMoveSpeed(int i)` — 从 PlayerPrefs 读取或返回默认值
- `set_LevelTime = 60` — 每阶段时间
- `set_LifeNum = 10` — 生命值
- `set_BlueNum = 20` — 蓝色方块数量
- `set_TarageNum[2]` — 每阶段目标数量

### SettingInGame_02 — Game02 游戏内设置

- **文件**: `Assets/Scripts/Game/GameSetting/SettingInGame_02.cs`
- **支持 5 个阶段速度设置**: `set_MoveSpeed[9]`（默认 `{1,2,...,9}`）
- **速度影响 LedAnim**: `trueSpeed = speed - set_MoveSpeed[JieDuan] * 5`

---

## 十三、全局速度公式汇总

| 游戏 | 变量名 | 公式 | 有效范围 | 受控于 |
|---|---|---|---|---|
| **Game01** | `max_runtime` | 迷宫方法内硬编码 | 0.5f ~ 2.0f | `Initmap_Maze_XX()` |
| **Game01** | `stayTime` | Up/Down 移动后固定 1.0f | 0 ~ 1.0f | `Game01_Ledone.Moving()` |
| **Game00** | `MaxrunTime` | `0.35f - 0.05f * GetMoveSpeed(JieDuan)` | 0.05f ~ 0.35f | `SettingInGame_01` |
| **Game02** | `MaxrunTime` | `0.8f - 0.05f * set_MoveSpeed[JieDuan]` | 0.05f ~ 0.8f | `SettingInGame_02` |
| **Game03** | 无全局速度 | `ChangeTarage_Time` + `tobeachtime[]` 独立计时 | — | 关卡代码 |
| **Game04** | 无全局速度 | `MaxRedStayTime(4f)` + `EyesWaitTime(10f)` | — | 关卡代码 |
| **Game05** | 无全局速度 | `SafeTime(1f)` + `Checking_CountTime` | — | 关卡代码 |
| **Game06** | `MaxrunTime` | `0.5f - 0.03f * Index_JieDuan` | 0.05f ~ 0.5f | 阶段索引 |
| **Game07** | `MaxrunTime` | `0.5f - 0.03f * Index_JieDuan` | 0.05f ~ 0.5f | 阶段索引 |
| **LedAnim** | `trueSpeed` | Game00: `speed - Index_JieDuan`; Game02: `speed - MoveSpeed*5` | ≥1 / ≥10 | 设置 + 阶段 |

---

> **注意**: 所有碰撞判定逻辑（`HitTarget`/`HitDie`/`CheckRxKey` 中的 `FjData.g_Fj[0].Life--` 等）为核心安全逻辑，**不可修改**。
