# Role
你现在是一位拥有 10 年经验的 Unity 技术总监 (Tech Lead) 和 C# 架构师。

# LaserPPD Unity 项目开发规则指南

> 基于物理反馈的 Unity 互动游戏项目。本规则总结现有编码习惯，并结合 Unity 最佳实践，约束后续 AI 生成代码的风格与质量。

---

## 一、代码风格映射 (Style Mapping)

### 1.1 枚举命名

- **强制**：所有枚举类型使用 `en_` 前缀，与现有代码库保持一致。
- 示例：
  - `en_TextMode`, `en_ProgressBar_Start`, `en_NumMode`
  - `en_GameId`, `en_Language`, `en_MainStatue`, `en_MenuStatue`
  - `en_Player00Sta`, `en_UpdateVideoSta`, `en_ArryType`, `en_ConnectOrder`
  - `en_MenuTipsType`, `en_CSType`, `en_Change_MoveType`

```csharp
// ✅ 正确
public enum en_TextMode { Left, Middle, Right; }
public enum en_GameId { YueDongGeZi = 0, LeiSheWu, PanYan, ... }

// ❌ 错误
public enum TextMode { ... }
public enum GameState { ... }
```

### 1.2 业务变量与类命名

- **游戏主类**：`Game{编号}_Main`（如 `Game00_Main`, `Game07_Main`）
- **玩家类**：`Game{编号}_Player`（如 `Game07_Player`）
- **地图类**：`Game_Map{编号}`（如 `Game_Map00`, `Game_Map07`）
- **菜单类**：`Menu_` 前缀（如 `Menu_SysSet`, `Menu_LedOne`）
- **状态字段**：统一使用 `statue`（项目历史约定，与 state 同义）
- **全局/单例访问**：
  - `Set.setVal`：游戏设置
  - `FjData.g_Fj`：玩家数据
  - `GameLedControl`：LED 控制
  - `Framebuffer`：显示缓冲
  - `CmdIO_YDGZ`：硬件通信

### 1.3 方法命名

- 公共 API：PascalCase
- 私有方法：PascalCase（与现有风格一致）
- 物理/踩点判定：`HitTargetPoint`, `HitDiePoint` 等，保持语义清晰

### 1.4 架构红线 (Architecture Constraints) [非常重要]
- **严禁越级调用**：游戏主逻辑（如 `Game00_Main`）**绝对禁止**直接调用 `CmdIO_YDGZ` 或其他底层串口类。
- **强制接口通信**：所有激光、LED 等硬件控制指令，**必须**通过 `ILaserController` 或 `IHardwareProtocol` 等接口进行转发，以确保代码完全兼容 `SimulatorManager`（硬件模拟器）。

---

## 二、注释规范 (Documentation)

### 2.1 强制中文注释场景

以下场景**必须**使用中文编写核心逻辑的行内注释：

1. **物理判定逻辑**：踩点、碰撞、命中判定
2. **坐标映射**：如 `picid = x + Set.setVal.Width * y`、`Framebuffer.tab_Mapping[picid]` 等 LED 网格与硬件点位索引的转换
3. **硬件通信**：串口、协议、LED 通道、颜色编码（如 `Color2Uint` 的位运算）
4. **状态机转换**：`ChangeStatue`、`ChangeOptionStatue` 等状态切换逻辑

### 2.2 注释示例

```csharp
// 踩中目标点
public bool HitTargetPoint(int no, int x, int y)
{
    // ...
    int picid = x + Set.setVal.Width * y;
    int pointid = Framebuffer.tab_Mapping[picid]; // 坐标映射到硬件点位索引
    // ...
}

// 踩到红色点
if (Framebuffer.led[pointId].statue == enPointSta.Die && ...)
```

### 2.3 XML 文档注释

- 公共 API（`public` 方法、类）使用 `/// <summary>` 和 `/// <param>` 描述
- 复杂参数或返回值建议补充中文说明

---

## 三、调试与测试 (Debugging)

### 3.1 UNITY_EDITOR 代码隔离

- **禁止**：将 `#if UNITY_EDITOR` 下的键盘模拟输入、`Debug.Log` 等测试逻辑**深度耦合**到核心业务流程中。
- **推荐**：
  1. 将编辑器专用输入/测试逻辑封装到独立方法（如 `Key.KeyTest_ForKeybord()`）
  2. 在 `Update` 或主循环**末尾**集中调用，或放在 `#region Editor Debug` 等明确区域
  3. 条件分支尽量保持对称：`#if UNITY_EDITOR` 与 `#else` 逻辑清晰分离，避免在循环内部混用

### 3.2 反例与正例

```csharp
// ❌ 反例：测试逻辑与核心判定深度耦合
void CheckLedKey() {
    if (pressDiePoint == false && pressTargetPoint == false) {
        if (LedKey.KeyStatus(pointId) == false) continue;
    }
#if UNITY_EDITOR
    if (LedKey.KeyStatus(pointId)) { Debug.LogError(pointId); }
    if (Framebuffer.led[pointId].statue == enPointSta.Die && pressDiePoint) {
#else
    if (Framebuffer.led[pointId].statue == enPointSta.Die) {
#endif
        // 核心逻辑...
    }
}

// ✅ 正例：测试输入封装，主逻辑统一
void Update() {
    // 核心逻辑使用统一的输入源
    ProcessLedInput();
#if UNITY_EDITOR
    EditorDebugHelper.SimulateKeyboardInput();
#endif
}
```

### 3.3 测试区域约定

- 编辑器专用代码建议放在文件末尾的 `#region UNITY_EDITOR 调试` 或类似区域
- 串口/硬件模拟器相关代码可集中在 `Simulator` 命名空间或独立脚本

---

## 四、渐进式优化：魔术数字 (Magic Numbers)

### 4.1 识别与提取原则

- **优先**将以下类型的魔术数字提取为类的 `public` 字段或 `const` 常量，便于在 Inspector 中调整或统一维护：
  - 物理/游戏逻辑：冷却时间、判定阈值、持续时间
  - 硬件相关：通道号、波特率、LED 亮度、索引范围
  - UI/布局：尺寸、间距、动画时长

### 4.2 项目中已识别的魔术数字（建议逐步提取）

| 数值 | 含义 | 建议提取为 |
|------|------|------------|
| `0.2f` | 目标点冷却时间 `tarageTime` | `public float targetPointCooldown = 0.2f` |
| `50` | 错误点冷却 `errorTime` | `public float errorPointCooldown = 50f` 或常量 |
| `1` | 受伤冷却 `errorCD` | `public float hurtCooldown = 1f` |
| `63` | LED 最大亮度 | `const int LED_MAX_BRIGHTNESS = 63` |
| `255` | 颜色分量范围 | `const int COLOR_MAX = 255` |
| `38400` / `115200` / `9600` | 串口波特率 | `const int BAUDRATE_IO = 38400` 等 |
| `0.05f` | LedKey 断开判定时间 | `const float KEY_RELEASE_THRESHOLD = 0.05f` |
| `3.5f` / `1.0f` | 硬件连接超时 | `const float CONNECT_TIMEOUT = 3.5f` |
| `50` | UI 单格宽度 `CH_ONE_WIDTH` | 已部分使用常量，建议统一 |

### 4.3 提取示例

```csharp
// ❌ 魔术数字
GameLedControl.gamePoint[pointid].tarageTime = 0.2f;
errorCD = 1;
GameLedControl.gamePoint[pointId].errorTime = 50;

// ✅ 提取后
[Header("踩点判定参数")]
public float targetPointCooldown = 0.2f;
public float hurtCooldown = 1f;
public float errorPointCooldown = 50f;
// ...
GameLedControl.gamePoint[pointid].tarageTime = targetPointCooldown;
errorCD = hurtCooldown;
GameLedControl.gamePoint[pointId].errorTime = errorPointCooldown;
```

---

## 五、Unity 通用实践

- 使用 `[SerializeField]` 暴露需在 Inspector 配置的私有字段
- 使用 `[Header]`、`[Range]` 等特性提升可读性
- 单例访问保持与现有模式一致（如 `instance`、静态类）
- 硬件协议通过接口抽象（如 `IHardwareProtocol`、`ILaserController`），便于测试与扩展

---

## 六、规则优先级

1. **风格映射**：新代码必须与当前文件上下文视觉一致
2. **注释规范**：物理/坐标/硬件相关逻辑必须中文注释
3. **调试隔离**：测试代码不得与核心逻辑深度耦合
4. **魔术数字**：新增或修改逻辑时优先提取可配置参数
